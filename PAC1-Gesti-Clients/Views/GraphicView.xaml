<UserControl x:Class="WPF_MVVM_SPA_Template.Views.GraphicView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPF_MVVM_SPA_Template.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="{DynamicResource MainBackgroundBrush}" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Graph type selector -->
        <DockPanel Grid.Row="0" Margin="0,0,0,10" LastChildFill="False">

            <Button DockPanel.Dock="Right"
            Style="{DynamicResource ModernPrimaryButtonStyle}"
            Content="←" Command="{Binding BackCommand}"/>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Tipus de gràfic:"
                   VerticalAlignment="Center"
                   Margin="0,0,10,0"
                   FontSize="{DynamicResource font-size-sm}"
                   Foreground="{DynamicResource SecondaryTextBrush}"/>

                <ComboBox ItemsSource="{Binding ChartTypes}"
                  SelectedItem="{Binding SelectedChartType}"
                  Width="120"
                  Style="{StaticResource ModernComboBoxStyle}"/>
            </StackPanel>

        </DockPanel>

        <!-- Graph -->
        <Border Grid.Row="1"
                Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource CardBorderBrush}"
                BorderThickness="1"
                CornerRadius="8">
            <oxy:PlotView Model="{Binding MyModel}"
                          Background="Transparent"/>
        </Border>
    </Grid>
</UserControl>